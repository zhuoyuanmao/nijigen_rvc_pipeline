###############################################################################
# Known-working dependency set (RVC v2 · 40kHz · RMVPE · ContentVec 768).
#
# torch is installed from the CUDA wheel index, e.g.:
#   pip install torch==2.5.1 torchaudio --index-url https://download.pytorch.org/whl/cu121
# The rest are the versions this pipeline was validated against. There is a
# triangular constraint between RVC (numba/numpy/fairseq), demucs (torch>=2),
# and audio-separator (torch>=2 + onnxruntime-gpu); if you let pip auto-resolve
# from scratch, pin torch first, then install the rest.
###############################################################################

# --- PyTorch (from the cu121 wheel index, NOT PyPI) ---
torch==2.5.1
torchaudio                # match your torch / CUDA build

# --- RVC training/runtime stack ---
numpy==1.26.4
numba==0.66.0
llvmlite==0.48.0
librosa==0.11.0
fairseq==0.12.2
faiss-cpu==1.7.3
pyworld==0.3.2
torchcrepe==0.0.20
praat-parselmouth>=0.4.2
einops>=0.8

# --- source separation ---
demucs==4.0.1                     # optional; v4.5 corpus prep uses Roformer instead
audio-separator[gpu]>=0.24        # Roformer/MDX separators; pulls onnxruntime-gpu
#   IMPORTANT: install ONLY onnxruntime-gpu, never onnxruntime (CPU) alongside it —
#   the CPU package shadows the GPU one and the CUDA provider disappears.

# --- audio I/O + DSP ---
soundfile>=0.12
scipy>=1.10
resampy>=0.4
pyloudnorm>=0.2

# --- training helpers ---
tensorboard
tqdm>=4.63
pydub>=0.25
scikit-learn>=1.1
Cython
